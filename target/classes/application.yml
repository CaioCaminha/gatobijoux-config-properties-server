server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/CaioCaminha/gatobijoux-configurations
          search-paths: product

